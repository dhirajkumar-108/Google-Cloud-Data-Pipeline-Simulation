# Core
pandas>=1.5.0
pyarrow>=10.0.0

# Google Cloud (install when you have a GCP project)
google-cloud-bigquery>=3.0.0
google-cloud-storage>=2.0.0
google-cloud-pubsub>=2.0.0

# PySpark (install Java 8+ first: sudo apt install openjdk-11-jdk)
pyspark>=3.3.0

# Airflow (install separately to avoid dependency conflicts)
# pip install apache-airflow
# apache-airflow[google]>=2.5.0   # for GCP operators (Cloud Composer)

# dbt (optional — for SQL-based transformation layer)
# dbt-bigquery>=1.5.0
